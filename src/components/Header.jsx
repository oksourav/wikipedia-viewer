import React from 'react';

const Header = (props) => {
    return (
        <header className="app-header">
          <h2>Wikipedia Viewer</h2>
          <h6>Search wikipedia article</h6>
        </header>
    );
};

export default Header;